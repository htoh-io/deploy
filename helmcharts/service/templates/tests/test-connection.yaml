apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "htoh-control.fullname" . }}-test-connection"
  labels:
    {{- include "htoh-control.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "htoh-control.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
