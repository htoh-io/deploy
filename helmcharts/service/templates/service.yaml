apiVersion: v1
kind: Service
metadata:
  name: {{ include "htoh-control.fullname" . }}
  labels:
    {{- include "htoh-control.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "htoh-control.selectorLabels" . | nindent 4 }}
